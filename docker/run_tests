 #!/bin/bash
 ./start_gradle_wrapper.sh
 ./gradlew firefoxTest

 if [ -n "${TEST_SUITE}" ]; then
  ./gradlew firefoxTest -Dsuite="${TEST_SUITE}"
 else
    echo "No TEST_SUITE variable set!";
    exit 1;
fi;